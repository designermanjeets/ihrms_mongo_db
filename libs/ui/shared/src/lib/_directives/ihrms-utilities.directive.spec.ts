import { MarginDirective } from './ihrms-utilities.directive';

describe('MarginDirective', () => {
  it('should create an instance', () => {
    const directive = new MarginDirective();
    expect(directive).toBeTruthy();
  });
});
