export const dateTimeLabelFormats = {
  millisecond: '%a, %e %b, %H:%M:%S.%L',
  second: '%a, %e %b, %H:%M:%S',
  minute: '%a, %e %b, %H:%M',
  hour: '%a, %e %b, %H:%M',
  day: '%a, %e %b, %Y',
  week: 'Week from %a, %e %b, %Y',
  month: '%b %Y',
  year: '%Y'
}
